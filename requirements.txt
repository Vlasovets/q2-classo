# Runtime dependencies are declared in pyproject.toml ([project] dependencies).
# This file exists only for `pip install -r requirements.txt`; prefer
# `pip install -e .` so the two cannot drift.
#
# Removed deliberately:
#   qiime2       -- would pip-install the framework INTO a conda QIIME 2 env
#   jupyter(lab) -- not a runtime dependency of the plugin

numpy>=2.0,<3
scipy>=1.11
scikit-learn>=1.3
numba>=0.60
pandas>=2.2,<3
zarr>=2.18.3,<3
numcodecs>=0.13,<0.16
plotly>=6,<7
c-lasso==1.0.11
scikit-bio>=0.6
